query($studentId: Long) {
    student(id:$studentId){
        id
        firstName
        lastName
        fullName
        learningSubjects {
            id
            subjectName
            marksObtained
        }
    }
}